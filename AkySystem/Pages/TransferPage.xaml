<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AkySystem.Pages.TransferPage"
             Title="Перевод">
    <VerticalStackLayout Padding="20" Spacing="15">
        <Entry x:Name="ToUserEntry" Placeholder="Кому (логин)" />
        <Entry x:Name="AmountEntry" Placeholder="Сумма" Keyboard="Numeric" />
        <Entry x:Name="MessageEntry" Placeholder="Сообщение (до 100 символов)" MaxLength="100" />
        <Button Text="Отправить" Clicked="OnSendClicked" />
        <Label Text="История переводов" FontAttributes="Bold" FontSize="18" Margin="0,20,0,0" />
        

        <CollectionView x:Name="HistoryList">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <HorizontalStackLayout Padding="5" Spacing="10">
                        <Label Text="{Binding Date, StringFormat='{0:dd.MM HH:mm}'}" WidthRequest="70"/>
                        <Label Text="{Binding AmountLabel}" WidthRequest="120" FontAttributes="Bold"/>
                        <Label Text="{Binding Direction}" />
                        <Label Text="{Binding Message}" FontSize="14" TextColor="Gray"/>
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>